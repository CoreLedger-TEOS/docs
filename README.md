# CoreLedger Docs

This repository contains the CoreLedger documentation site built with Docusaurus.

## Commands

```bash
pnpm install
pnpm run build
pnpm run start
```

The static build output is written to `docusaurus/`.

## Editing documentation

For editors who only need to update pages, add new pages, or add images, see [Editing guide](EDITING_GUIDE.md).
